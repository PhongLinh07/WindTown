## 1. Data Review

- [x] 1.1 Review `degsignUI.json` modules/forms and derive expected counts (total/done/remaining/modules)
- [x] 1.2 Map layout/status values to badges/classes used in `hrm_form_list.html`

## 2. HTML Sync

- [x] 2.1 Update summary totals in `hrm_form_list.html` to match JSON
- [x] 2.2 Update each module header (name, color, form count, tables list) to match JSON
- [x] 2.3 Update each form card (name, tables, description, layout badge, status badge/class) to match JSON

## 3. Verification

- [x] 3.1 Re-scan `hrm_form_list.html` to confirm all modules/forms match JSON and counts are consistent
